package com.example.galerio.data.service

import com.example.galerio.data.model.MediaItem

class PhotoService {
    fun getPhotos(callback: (List<MediaItem>) -> Unit) {
        // Lógica para obtener las fotos desde el servidor y llamar a la devolución de llamada con los datos
    }
}